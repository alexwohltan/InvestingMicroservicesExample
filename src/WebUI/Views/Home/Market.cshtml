@model ExternalDataStructures.MarketView

@{
    ViewData["Title"] = "Market Page";
}

<div class="text-center">
    <h1 class="display-4">@Model.MarketData.Name Market</h1>
</div>

<div>

    <div id="Sectors" class="visible">
        <h3 class="display-6">Sectors</h3>
        <table id="SectorTable" class="table table-hover">
            <tr>
                <th>ID</th>
                <th>Market</th>
                <th>Name</th>
                <th>Action</th>
            </tr>
            @foreach (var sector in Model.MarketData.Sectors)
            {
                <tr>
                    <td>@sector.ID</td>
                    <td>@Model.MarketData.Name</td>
                    <td>@sector.Name</td>
                    <td><a href="Sector?marketName=@Model.MarketData.Name&sectorName=@sector.Name">View</a></td>
                </tr>
            }
        </table>
    </div>

    <hr />
    <div>
        <h3 class="display-6">Benchmark</h3>
        @await Html.PartialAsync("_Benchmark", Model.BenchmarkData)
    </div>
    
    
</div>